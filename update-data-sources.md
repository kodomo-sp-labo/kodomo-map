# データソース定期確認・更新タスク

あなたはこのリポジトリの資源マップ（岡山県こども支援資源マップ）のデータ鮮度を保つ担当です。
以下の手順を **順番通り、逸脱せず** 実行してください。最終的な成果物は
**Pull Requestの作成のみ**で、mainブランチへの直接コミット・マージは絶対に行わないこと。

---

## 手順

### 1. 出典台帳を読む
リポジトリ直下の `sources.json` を読む。

**【2026-07-26改訂】台帳は全国版（`_schema_version: 2`）に変わった。** 構造は次の2階層。

- `sources`: 出典の実体。キーが出典ID（例 `wamnet`, `cfa_sodan`, `mext_tayouka`,
  `okayama_pref_momokko`）。`source_label` / `source`（一覧ページURL） / `checked` /
  `as_of`（原典の基準時点） / `publisher` / `tier` / `discovery_query` / `notes` を持つ。
  **`source` が `null` のものは一覧ページURLが未確認**であることを意味する（推測でURLを
  補わない。CLAUDE.mdルール2）。監視対象にする前に、まずURLの特定を人に依頼すること。
- `datasets`: どのファイルがどの出典から作られるかの定義。`files`（対象パス）、
  `record_src`（各レコードの `src` に入れる出典IDの決め方）、`fallback_src`（その県が
  0件だったときに表示する出典）、`last_count` / `last_checked` / `monitor` を持つ。

`data/` 配下の各JSONは**出典IDだけ**を `src` フィールドに持ち、ラベル・URL・確認日は
持たない（出典文字列の二重管理を避けるため）。したがって**出典を直すときは
`sources.json` だけを直せばよく、データファイルを触る必要はない**。
`index.html` は起動時にこの台帳を読み、フッターの出典表示を組み立てている。

処理するのは `monitor: true` のデータセットのみ（2026-07-26時点では全て `false` ＝
月次自動更新は未稼働）。

### 2. 各ソースについて「一覧ページ」の生存確認
`sources.<出典ID>.source` のURLをfetchする（`null` のものはスキップし、URL未特定である
旨をPRに記載する）。

- **HTTPステータスが200以外、またはリダイレクト先が明らかに無関係なページの場合**：
  そのソースの処理を中断し、「出典ページが失われている可能性」として記録する（後述の
  discovery_queryでの検索に進む）。推測でURLを組み立てて別のページを試すことは禁止。

### 3. 一覧ページから、現在の個別データファイル（PDF等）のリンクを抽出
一覧ページの中身を読み、対象データ（フリースクール一覧、児童福祉施設一覧等）への
リンクを特定する。**このリンク自体は保存・固定しない**（sources.jsonにも書き込まない）。
あくまで今回の実行で使う一時的なURLとして扱う。

### 4. データを取得・パースし、新しいJSONを生成
既存の `output_file`（例: `data/okayama/hattatsu.json`）と同じスキーマ（フィールド名・
形式）で、新しいデータを生成する。生成後、以下の異常検知を必ず行う：

- **新しい件数が、`sources.json` の `last_count` と比べて大きく変動していないか**
  （目安：50%以上の増減、または0件・1件など極端な値）。異常があれば、
  **JSONファイルを上書きせず**、その旨をPRの説明に明記して人の確認を求める。
- 既存のJSONと新しいJSONで、フィールドの欠落・型の変化がないか。

異常がなければ、`output_file` を新しいデータで上書きする。

### 5.（今回追加）出典ページ自体が現在も「最新」かを検索で二重チェック
`discovery_query` でWeb検索を行う。上位結果に `list_page` と異なるURLが出てきた場合：

- そのURLを**自動的に採用しない**。
- PRの説明に「検索結果ではより新しい可能性のあるページが見つかりました：
  `<候補URL>` （現在の登録: `<list_page>`）。ご確認のうえ、正しければ
  `sources.json` の `list_page` を手動で更新してください。」と明記するだけに留める。
- 検索結果が現在の `list_page` と一致する、または明確に無関係（ニュース記事等）の
  場合は何も記載しなくてよい。

### 6. sources.json の更新
処理した各データセットについて、`datasets.<名前>.last_checked` を今日の日付に、
`last_count` を今回取得した件数に更新する（件数が異常で採用を見送った場合も、
`last_checked` は更新し、`notes` に「件数異常のため今回は未反映」と追記する）。
出典そのものを確認し直した場合は `sources.<出典ID>.checked` も更新する。

**レコード側の `src`（出典ID）を変える必要がある場合は、`メモ等/apply_sources.py` を
使う**（手作業で各JSONを書き換えない）。このスクリプトは冪等で、件数が変化したら
停止する。

### 7. Pull Requestの作成
- ブランチ名: `auto-update/YYYY-MM-DD`
- タイトル: `データソース定期確認（YYYY-MM-DD）`
- 本文には、ソースごとに以下を簡潔にまとめる：
  - 変更なし / 更新あり（件数の変化） / 異常検知のため見送り / 出典ページ消失
  - 出典ページ消失または件数異常の場合は、その詳細と、見つかった代替候補（あれば）
- **変更が一切なかった場合はPRを作成しなくてよい**（無駄な通知を避ける）。

---

## 絶対に守ること

1. mainブランチへの直接コミット・マージは禁止。必ずPR経由。
2. 個別データファイルの直リンク（PDF等）を `sources.json` に書き込まない
   （毎回一覧ページから再取得する設計を壊さないこと）。
   **例外として `okayama_pref_jidoukan` は暫定でPDF直リンクを登録している**（親の
   一覧ページを特定できていないため）。監視対象にする前に一覧ページを特定すること。
3. 件数異常・出典消失を検知したら、**推測で穴埋めしたり、古いデータをそのまま
   「正常」として扱ったりしない**。必ずPRで人に判断を委ねる。
4. 検索で見つけた代替URLを、確認なしに正式な出典として採用しない。
5. ②居場所系（ibasho_places）は元データの構造が複雑なため、自動生成した内容に
   自信が持てない場合は、無理に上書きせず「差分の可能性あり、手動確認を推奨」と
   PRに記載するだけに留めてよい。
