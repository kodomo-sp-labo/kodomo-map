# こども支援資源マップ — Claude Code運用ルール

## プロジェクト概要
不登校・発達支援等の公的資源を地図表示する静的サイト（GitHub Pages）。
構成: index.html（SPA本体）+ data/<都道府県コード>_<ローマ字>/*.json
      （例: data/33_okayama/）。スクリプト・補助データは メモ等/ に格納。
index.html内のREGIONSに県を登録すると表示される。

## 【重要】作業拠点は2箇所ある
- 編集・データ生成: このフォルダ（OneDrive、子どもウェルビーイングlabo HP）
- git commit・push: C:\Users\user\kodomo-map
  （GitHubの既存リポジトリ kodomo-sp-labo/kodomo-map をcloneした別フォルダ。
  2026-07-25、実際のPC上でTest-Path・git remote -vにより実在・同一リポジトリ
  であることを確認済み。旧記載のC:\Users\Owner\Desktop\_check\kodomo-map-git
  はこのPCには存在しない別環境の記載だったため修正）
このフォルダ自体はgit管理されていない。commitを求められたら、まずこのフォルダの
変更点（data/・index.html等）を上記クローンにコピーしてから、クローン側でcommitする。
このフォルダで`git init`は絶対に実行しない（既存リポジトリと無関係な別履歴になる）。

## 絶対に守ること
1. 公的一次資料主義: 出典は公的機関のみ。民間集約サイトは使わない。
2. 推測でデータを埋めない: 取得できない値はnull/unresolvedにして人に報告する。
   住所・座標・電話番号を「それらしく」補完することは重大な誤りである。
3. 件数異常検知: パース結果が0件、または既知の想定から大きく外れたら
   自動で先に進まず、停止して報告する。
4. 公開反映（REGIONSへの登録・mainへのpush）は必ず人の承認を得る。
   push自体もClaude Code経由では認証できないため、運営者自身が別ターミナルで
   直接実行する。commitはClaude Codeが行ってよいが、内容（メッセージが実態と
   一致しているか、既存データの置き換えなら中身のサンプル確認をしたか）を
   必ず確認してから行う。
5. 外部サイト取得時は1リクエスト/秒以上のウェイトを入れる（既にスクリプトに実装済み）。
6. 出力JSONには source_label / source / checked（確認日）を必ず含める。
7. 精度の明示: 座標が市区町村レベル概算のものは prec="area" を付け、番地精度を主張しない。
8. 都道府県フォルダ名は必ず pref_codes.py の folder_name() で決定する
   （「01_hokkaido」〜「47_okinawa」形式）。独自にenumerate等で連番を振らない
   （cfa.go.jp索引順・辞書順は都道府県コード順と一致しないため事故のもと）。
9. ジオコーディング処理は「座標が取得できたか」だけでなく「取得した座標が
   期待する都道府県・市区町村を実際に指しているか」を必ず突合検証すること。
   geocode_matched等の照合結果文字列に、期待する都道府県名・市区町村名が
   含まれるかを機械的にチェックし、含まれない場合は解決済み扱いにしない
   （郡どまり・複数箇所・地名でない値ほど、一見成功して見える誤りが起きやすい）。
10. data/やindex.htmlを一括で書き換える作業（③の47県再生成、REGIONS変更、
    フォルダの一括リネーム等）の前には、必ず`python メモ等/backup_data.py`を
    実行してタイムスタンプ付きバックアップを取ってから作業を始める。
    軽微な単発の修正（1ファイルの文言修正など）では不要。
11. 一見矛盾するデータ（例: 行政区域コードと運営者名が違う自治体を指しているように
    見える等）ほど、修正する前に一次資料（Wikipedia・自治体公式サイト等）で裏取りする。
    「想定と違う」は即「データが間違っている」ではない。越境通学・離島自治体の役場
    所在地など、正当な行政上の理由で一見矛盾して見えるデータが実在する。
12. 新しい都道府県を追加した後の動作確認は、岡山県だけでなく、岡山県から地理的に
    離れた県（本州から離れた沖縄・北海道、面積や施設密度が極端な県等）を最低1つ
    含めること。「岡山県だけを見て正常」は実装の正しさを保証しない
    （実例: ⑤発達タブが岡山県以外で常に誤表示していたバグが、岡山県だけの確認では
    発見されなかった）。
13. 凡例・注記に地名や件数を静的に書かない。実データから算出して埋めること
    （例: updateSodanLegend()・updateOyaLegend()）。静的な文字列はデータが増えても
    地域が変わっても更新されないため、必ず実態と乖離する。実際に3件発生している
    （④「岡山市58件」＝実際は39件で58だった時期は一度も無い、③「教育相談室（岡山市・
    番地あり）」＝全47都道府県に岡山市と表示、①「公設・岡山市」＝他県では制度が違う）。
14. 地名や件数が固定されているバグを直すときは、その文言が説明している対象が
    実際に存在するかまで確認する。文言だけ直すと「正しい地名で、存在しない機能を
    説明する」状態になる（実例: ③の凡例が指す個別ピンは全47都道府県で0件だった）。
15. 「未修正と記録されている」と「実際にまだ直っていない」は別物。弱点リストや
    メモの記載を信じて修正に着手せず、先に実ファイルの現在の状態を確認する
    （実例: 仕様書が未修正としていた④凡例の児童館欠落は、別作業のついでに修正済みだった）。
    逆に、修正したら記録側も同じ作業の中で更新する。
16. 自分の分類・判定の結果が想定と違うときは、データではなく自分の判定ロジックを
    先に疑う（実例: 県PDFの「主たる障害種別」列の有無でセンターを判定したため、
    同じ列を持つ障害児入所施設6件をセンターと誤認していた。着手前の自己検証で
    誤ったデータを本番に入れる前に発見できた）。

## データスキーマ
既存の岡山版JSONのフィールド構成を全国標準とする。変更が必要な場合は先に相談。