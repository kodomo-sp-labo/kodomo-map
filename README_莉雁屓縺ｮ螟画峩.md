# ⑤発達 全国展開・本番反映パッケージ（2026-07-24）

## 内容
- `index.html` … ⑤発達タブをcat配列・5分類対応に修正済み（要置き換え）
- `data/okayama/hattatsu.json` … 405件（WAMNET由来403施設＋既存PDFとの突合でp/d/dis/pref_pdfを付与）
- `data/<他46県>/hattatsu.json` … WAMNET由来のみ（新規。REGIONSに未登録のため今はまだ表示されない）
- `hattatsu_removed_okayama.json` … 既存393件のうちWAMNETで確認できなかった28件（削除ログ、要人手確認）
- `convert_wamnet_hattatsu.py` … WAMNET CSV→都道府県別JSON変換スクリプト（次回3月末/9月末更新でも再利用可）

## 反映手順
1. リポジトリの `index.html` をこの版に置き換える
2. リポジトリの `data/okayama/hattatsu.json` をこの版に置き換える（393→405件、スキーマ変更あり：cat配列化、pのみ一部欠損の可能性なし＝既存分は全てpあり、新規40件はpなし）
3. `hattatsu_removed_okayama.json` の28件に目を通し、廃止確認が取れたものは何もせず、現存が疑われるものがあればWAMNET未報告の可能性として個別確認（sources.json運用方針の「推測で穴埋めしない」原則により、自動では復元していません）
4. ブラウザで実機確認（フィルタ5種類の チェックボックスON/OFF、マーカー色、クラスタ表示、カード一覧、ポップアップの「県公表資料でも確認済み」リンク）
5. 問題なければコミット→PR→レビュー→マージ（mainへの直接コミットは避ける、update-data-sources.mdの運用方針を踏襲）
6. 他都道府県分（data/<pref>/hattatsu.json）はまだREGIONSに未登録のため、公開時期が来たらREGIONSに1エントリ追加するだけで表示される

## 未解決・要確認事項
- WAMNET基本CSVには「児童発達支援センター」指定・「重症心身障害児対象」の判定フラグが含まれないため、新規捕捉40件についてはdis（対象種別）情報が空です。必要であれば運営情報側のより詳細なデータ、または個別確認で補完してください。
- 郵便番号(p)は新規40件に存在しません。表示上「〒 」の後が空になります（大きな崩れではないですが見た目の確認をお願いします）。
